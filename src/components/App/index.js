import React, { Component } from 'react';
import styles from "./styles.module.css"

class Page extends Component {
    render() {
        return (
            <div>base</div>
            // layout
        );
    }
}

export default Page;